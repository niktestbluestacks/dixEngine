{
    "configurations": [
        {
            "includePath": [
                "${workspaceFolder}/**",
                "${workspaceFolder}/Dependecies",
                "$(workspaceFolder)/VulcanEngine",
                "$(workspaceFolder)/Applications"
            ]
        }
    ]
}